# Decision Log

| 日付 | 決定 | 理由 | 代替案 |
|---|---|---|---|
| 2026-09-19 | repo 名を `e-nexus-decision-layer` とし Jev 名を含めない | Jev は Adapter の1つ。本体を Jev 依存にしないため | `jev-gateway` 等 |
| 2026-09-19 | Vault 外の独立 repo（`C:\Users\envie\e-nexus-decision-layer`）に置く | Claude Code 専用にしない。Cursor / Hermes / 販売App から同じ CLI・SDK を使う。Vault は正本（設計・判断ログ）だけを持つ | Vault 内 `app/`、en-generate-hub 内サブモジュール |
| 2026-09-19 | Node ESM・依存ゼロ・`node --test` | en-generate-hub / en-product-hub と同じ idiom。install 不要で許可プロンプトを増やさない | TypeScript + vitest |
| 2026-09-19 | deterministic rules を chain の先頭に固定 | 監査で見つかった既存判断（Advisor 13条件・AI-Company §6 表・Plan 判定表）はほぼ決定的。Jev は「rules で解けない残り」だけに使う | Jev を先に呼ぶ |
| 2026-09-19 | tier 語彙を auto / review / human | en-product-hub の pull-plan（auto / claude / human）と概念を揃える | high / medium / low |
| 2026-09-19 | outcome から承認キーを構造的に排除（forbidden_outcome_keys + closed schema + test） | 「Human Gate を弱めない」を主張ではなく機械で担保する | ドキュメントで禁止するだけ |
| 2026-09-19 | 高コストLLM provider（anthropic / openai / google）の Adapter は `allow_paid_adapters=true` のときだけ chain に残す。**Jev は対象外**（既定で呼ぶ） | Jev の役割は「高額AIを呼ぶ前の低コストGate」。Cost Gate で止めると存在意義が消える（Advisor指摘で修正）。LLM は Medium confidence の再判定用なので opt-in | Jev も cost-gate する |
| 2026-09-19 | 「Humanへ上げる」フラグ名を policy（`force_human_when_outcome_keys`）で一元管理 | ドメインごとに語彙が違う（`human_review_required` / `needs_human_review` / `human_required`）。engine にハードコードすると schema-only の decision_type で human 強制が効かない（Advisor指摘で修正） | 語彙を1つに統一して各schemaを書き換える |
| 2026-09-19 | `__mock` は `allowMockControl=true`（テスト専用）のときだけ有効 | 本番入力から Mock の経路を操作されないため | schema から `__mock` を外す |
| 2026-09-19 | PoC は「有料生成直前の Decision Gate」 | docs/poc-paid-generation-gate.md 参照。Model Router は rules で完結するため Jev PoC に不向き | Model Router / Skill Router |
| 2026-09-19 | metering の通貨単位を USD micros | en-generate-hub budget.mjs と同一単位で転記時の桁ズレ防止 | USD float |
| 2026-09-19 | `EN-Volt` は登録しない | Vault・ホームディレクトリのどこにも実体が確認できなかった（推測で作らない） | 空エントリ登録 |
| 2026-09-19 | Jev Adapter を「変換」と「経路（Provider）」に分離。`JEV_PROVIDER`＝direct / vercel / cloudflare | Adapter が env 直読み・direct 固定だと経路追加で decide() を書き換えることになる。Provider 1ファイル追加で済む形に | Adapter を経路ごとに複製 |
| 2026-09-19 | 将来ユースケース4件（micro decision / context relevance / I/O guard 補助 / post-execution verify）は decision_type 名の**予約のみ**（schema・rules・Adapter は作らない） | 名前と責務境界を先に固定し、実装時に既存設計と矛盾しないようにする。特に guard 補助は「解除を返せない」禁止キーを先に置く | 今 schema まで作る |
| 2026-09-19 | ベンダー公表の性能値（ms・価格比・選択肢上限・学習手法等）を仕様に入れない | 変更され得る値に本体を依存させない。閾値は実測で決める | docs に数値を転記 |
| 2026-09-19 | Direct Provider は `@typesafe-ai/sdk` を採用せず、Node 20+ の `fetch`/`AbortController` で自前実装 | 依存ゼロ方針（`package.json`に依存無し）を維持するため。SDK既定値（timeout 10s・retry 2回・backoff・Retry-After尊重）は自前実装で再現 | SDK採用（依存追加を許容） |
| 2026-09-19 | outcome schemaのフィールド型からJev questionsへ自動写像（boolean→noul、enum文字列→choice、2〜10段の整数→score）。対応不能な型は`JEV_UNSUPPORTED_OUTCOME_FIELD`で止める | decision_typeごとにJev変換コードを書き足す設計だとAdapterが肥大化し、型の対応漏れを推測で埋めるリスクが出る。汎用写像＋明示的な非対応エラーの方が安全 | decision_typeごとに専用マッピング関数を書く |
| 2026-09-19 | 全体confidence = 各questionのconfidenceの最小値（保守側で合成） | 一部のquestionだけ確信度が高くても、他が不確かなら全体をhuman行きにする。楽観的な平均・最大値は誤ってautoにする方向に倒れやすい | 平均値・重み付け合成 |
| 2026-09-19 | noulのconfidenceを`\|2*noul-1\|`として独自導出（公式は返さない） | 公式値が無い以上ここで作るしかない。0.5付近＝五分五分で低confidence、0/1付近＝高confidenceという直感に一致させた | confidence常に1.0固定（楽観） |
| 2026-09-19 | Vercel AI Gateway Provider（`jev-vercel-provider.mjs`）を追加。`jev-adapter.mjs`（変換層）は変更しない | Gateway の Evaluation modality は AI SDK 専用（REST互換エンドポイントには無いと公式に明記。vercel.com/docs/ai-gateway/modalities/evaluation）で、質問型（boolean/choice/score・confidenceの位置）が Direct のTypeSafe生API形式と異なる。Provider層に「経路＋相互変換」を閉じ込めれば Adapter・Engine は経路追加のたびに書き換えずに済む | jev-adapter.mjs にVercel固有分岐を追加する |
| 2026-09-19 | 依存パッケージ `ai`（AI SDK v7）は `dependencies` ではなく `optionalDependencies` に置き、`jev-vercel-provider.mjs` 内で動的 `import('ai')` する | Decision Layer全体の依存ゼロ方針（Direct Provider参照）を維持しつつ、Evaluation modalityがAI SDK経由でしか提供されない（REST代替なし）制約を両立させる。未インストールでも direct/mock/rules経路・既存69testsに影響させない（`JEV_VERCEL_SDK_MISSING`でunavailableに落ちるだけ） | `dependencies`に追加し`engines.node`を22+へ一括で上げる |
| 2026-09-19 | `package.json`の`engines.node`は`>=20`のまま変更しない（vercel経路だけがNode 22+必須） | AI SDK 7がNode 22+必須なのは事実だが、`ai`をoptionalDependencyにした以上、direct/mock/rules専用で使う環境まで巻き込んで底上げする理由が無い。vercel経路を使う環境だけが実質的にNode 22+を要求される（.env.example・docs/architecture.mdに明記） | engines.nodeを>=22へ変更 |
| 2026-09-19 | Vercel側の認証は `process.env.AI_GATEWAY_API_KEY` への暗黙依存ではなく `createGateway({ apiKey: env.AI_GATEWAY_API_KEY })` で明示注入する | Decision Layerの全Providerが env を明示的に受け取る設計（Direct は Authorization ヘッダーを都度組み立てる）と揃える。ambientなprocess.envに依存すると、envを明示的に渡すテスト・将来のマルチテナント実行と食い違う | 素の文字列モデルID（`'typesafe-ai/jev'`）を渡し、SDKの既定（process.env読み取り）に任せる |
| 2026-09-19 | `createGateway({apiKey}).evaluationModel(modelId)`が実在する前提で実装し、`resolveEvaluationModel()`で存在チェックのみ入れて確定は次回の実疎通確認に委ねる | 一次情報（vercel.com/docs/ai-gateway/modalities/evaluation）で確認できたのは既定シングルトン`gateway`（'@ai-sdk/gateway'）の`.evaluationModel()`のみ。`createGateway()`が返すカスタムインスタンスが同じ形を持つことはAI SDKの他Provider（createOpenAI等）との設計一貫性からの推定であり、一次情報での直接確認はできなかった。関数が無ければ`JEV_VERCEL_SDK_ERROR`で即失敗し、推測成功を偽装しない | 未確認のまま断定してドキュメント化する／実装を保留する |
| 2026-09-19 | Vercel経路のconfidence欠落（choice/score）はprobabilitiesの最大値等から合成しない。`parseJevResponse`のJEV_MALFORMED_RESPONSE判定に委ねる | 公式ドキュメントが明記：`providerMetadata.typesafe.confidence`は「選択肢の確率でも、Provider横断の指標でもない」。probabilitiesから独自の疑似confidenceを作ると、Direct/Vercelで意味の異なる値をconfidenceとして混在させることになる | probabilitiesの最大値をconfidence代わりに使う |
| 2026-09-19 | Vercelの `evaluate()` 内蔵retry（既定maxRetries=2）をそのまま使い、Direct Providerのような自前リトライループを重ねない | AI SDKが既にexponential backoffでリトライする（一次情報：ai-sdk.dev/docs/reference/ai-sdk-core/evaluate）。二重にリトライを重ねると合計試行回数が意図せず膨らみ、無限retry禁止の指示にも反する方向に倒れやすい | Directと同じ手動リトライループをVercelにも実装する |
| 2026-09-19 | Vercel経由Jev**実疎通成功**を受け、`createGateway({apiKey}).evaluationModel(modelId)`の実在を「推定」から「確定」へ更新 | `JEV_PROVIDER=vercel`・`typesafe-ai/jev`・`ai@7`インストール済みで実通信成功（約3.1s、confidence≈0.08）。この経路は evaluateImpl 注入なしの本番パスなので、成功＝当該メソッド実在の実証 | — |
| 2026-09-19 | Vercel Provider の既定モデルIDを `typesafe-ai/${内部ID}` 接頭辞方式から正規ID `typesafe-ai/jev` 固定へ変更。`JEV_VERCEL_MODEL` 上書きは維持、Direct の `jev-latest` は不変 | Gateway のカタログ（`typesafe-ai/jev`）と TypeSafe Direct のカタログ（`jev-latest`／`jev-preview`）は別物。実疎通で成功したのは `typesafe-ai/jev` であり、`typesafe-ai/jev-latest` は未検証。未検証IDを既定にしない | 接頭辞方式を維持し `jev-latest` を Gateway 側で解決させる |
| 2026-09-19 | **mock-jev は実 Jev 経路が使えるとき既定 chain に入れない**（`src/index.mjs` `defaultAdapters` + `realJevUsable(env)`）。core（fallback/engine）は変更しない | 実疎通で、実 Jev の正常応答（0.08・tier=human）の直後に mock（photoreal ヒューリスティック 0.88）が chosen になり、最終 `resolved_by=mock-jev`／`provider=mock`／cost 0 と、課金済みの実呼び出しを表さない結果になった（usage.jsonl で確認）。SSOT§6 の mock の役割は「APIキー無しで本体を検証」であり、実 Provider の代替 fallback ではない。chain 構成で直す方が「Engine は Jev を知らない」原則を守れる | fallback.mjs に「実 Provider の ok は最終」ルールを入れる（core に Adapter の身元判断が入る）／`kind:'mock'` を追加 |
| 2026-09-19 | fallback.mjs の「ok だが human 相当なら次の Adapter へ進む」継続設計は**変更しない** | docstring・architecture §2-5・既存テストで明示された正式設計で、llm Adapter（Medium confidence 時の再判定差し込み口）の前提でもある。今回の欠陥は継続そのものではなく mock が割り込む位置の問題 | 正常応答＋低confidence で chain を止め、Jev の outcome を tier=human で返す（設計変更。llm 再判定経路が死ぬ） |
| 2026-09-19 | 残課題として記録（今回は修正しない）：chain 継続時、実 Jev の confidence／usage が `result`・metering に出ない（trace のみ）。課金された実呼び出しが usage.jsonl に載らない | metering は「1判定=1行、chosen の Adapter だけ」設計。直すには metering 設計の変更（試行ごとの行、または trace 由来の合算）が要り、今回の最小修正の範囲を超える | 今回まとめて metering を変更 |
| 2026-09-19 | Vercel Provider で 403 を 401 と分け `JEV_FORBIDDEN`（再試行なし）にする。Direct の 403→`JEV_HTTP_ERROR` は今回触らない | 実作業で 403 がカード未認証・モデル権限・Gatewayポリシーでも返ると分かり、`JEV_AUTH_FAILED` に混ぜると原因追跡を誤らせる。response body はログへ出さない | 401/403 を同一扱いのまま |
| 2026-09-19 | `package-lock.json` を commit する（`ai@7` を optionalDependencies のまま固定） | Human の `npm install` 結果を再現可能にする。注意：`npm ci` するマシンでは `ai@7` が入るため、その経路では実質 Node.js 22+ が要る（`engines.node: >=20` は direct/mock/rules だけで使う環境向けの下限として残す） | lock を commit しない |
| 2026-09-19 | CLI／SDK は `.env` ファイルを読まない（依存ゼロのまま）。実行時の env はシェルに export された値だけ | `realJevUsable()` と jev adapter は同じ `process.env` を見るので、シェルにキーが無ければ jev unavailable → mock が残り、あれば jev が呼ばれ mock は外れる、と常に整合する。`.env` を読む機構を足すと Secret の読み込み経路が増える | dotenv 相当の自前ローダーを追加 |
| 2026-09-19 | mock-jev除外修正の効果を実測で確認済みとして記録：Human が `JEV_PROVIDER=vercel`・`AI_GATEWAY_API_KEY` 設定済みシェルで `decide --file request-vercel-test.json` を再実行し、`mock-jev: NOT_REGISTERED`（chainに入らない）・`jev: ok, confidence≈0.08, tier=human, latency 881ms`・最終 `resolved_by: human` を確認 | 前回修正（`realJevUsable`）が unit test だけでなく実環境でも意図どおり動くことを一次情報で確認するため。latency は初回実疎通（約3.1s）より短く、単発計測なのでベンダー性能値として仕様化しない | unit test の合格のみで実環境確認を省略する |
| 2026-09-19 | `request-vercel-test.json`（実疎通用リクエスト、秘密情報なし）を repo へ commit し、`docs/poc-paid-generation-gate.md`から参照する | 一時ファイルのまま放置せず、今回発見した mock 上書き欠陥の再現手順として今後も使う価値がある。新しい `tests/fixtures/` 等のディレクトリ構成は今回作らない（スコープを広げない） | 削除する／untracked のまま放置する |
| 2026-09-19 | 途中adapter（今回の real Jev 等）の usage／confidence／latency が最終 result・usage.jsonl に残らない残課題を、次タスク `MA-30 Intermediate Adapter Metering` として名前を確定（実装はしない） | 「今回の作業範囲で直すもの」と「次タスクへ引き継ぐもの」を区別するため、Vault側実行タスク台帳・本ログ双方から追える名前を今のうちに固定する。設計候補（`attempts[]` 等）はここでは決めない | 今回のセッションで設計まで進める |
| 2026-09-19 | **Intermediate Adapter Metering 実装**：final result（resolved_by/provider/model/top-level usage）の意味は変えず、`usage.jsonl` の同じ1行に `attempts[]`（全 attempt record）と `usage_total`（合計。final 分を含む）を**追加**する。attempt を別行にしない | 別行にすると `summarize()` の requests / fallbacks / human_escalations が decision 数でなくなり既存 consumer が全部ずれる。nested なら旧行は読めたまま migration 不要、`summarize` の既存フィールドも無変更で済む | attempt ごとに別 JSONL 行／top-level usage を全 attempt 合計に変更（意味が変わる） |
| 2026-09-19 | attempt record は `fallback.mjs` が1か所で作り、`result.fallback.trace` と metering の `attempts[]` が同じ record を共用する（`attemptsFromTrace()` は `ms` を落とす射影のみ） | 損失点は fallback の trace 生成（ok 側が `result.usage` を捨てていた）。そこで作れば表示・metering・デバッグが矛盾しない。大規模リファクタリングを避ける | metering 側で trace とは別に attempt を再構成する（二重管理） |
| 2026-09-19 | `networked`（外部 provider へ実際に送ったか）は throw / return する側（Provider・Adapter）が決め、core は reason 文字列の allowlist で推定しない | 「Engine は Jev を知らない」原則。Provider が送信後の失敗（401/403/422/429/5xx/timeout/応答不正）に `details.networked=true` を付け、送信前ゲートは付けない（core は false 扱い）。generic error と判別不能な SDK エラーは `null`（不明）。課金対象通信を「起きた」と偽装しない | core に reason → networked の対応表を置く／すべての unavailable を networked 扱い |
| 2026-09-19 | usage が取れない失敗 attempt は `usage_known:false`・tokens/cost `null` とし、`usage_total.unknown_usage_attempts` に数える。0 と書かない | 0 は「無料」、null は「不明」。合計に unknown を混ぜると実コストを過小報告する。送っていないと確定できる attempt だけ 0 を known として書く | 失敗は一律 0 |
| 2026-09-19 | 1 attempt = `adapter.decide()` 1回。Provider 内部 HTTP 再試行は `retry_count` で持ち attempt を増やさない。Direct は実カウント（`send({meta})` に書き戻し）、Vercel は AI SDK 内部で観測不能なので `null` | 二重計上防止。観測できない回数を 1 や 0 と決め打ちしない | 再試行ごとに attempt 行を追加／Vercel の retry_count を maxRetries から推定 |
| 2026-09-19 | attempt record の `model` は実応答のモデルID（Jev の `raw.model`／Vercel の `response.modelId`）を優先し、無ければ `adapter.model` | AI Cost Manager 連携・課金監査では「どのモデルに課金されたか」が要る。静的な adapter.model（`jev`）だけでは不足 | adapter.model 固定 |
| 2026-09-19 | mock-jev の模擬トークン（input_tokens>0・cost 0）は従来どおり出す。`provider:'mock'`・`networked:false` で real provider usage と分離する | 既存テスト（`rows[1].input_tokens > 0`）と「原価管理の配管が動くことを確認する」役割を維持。発注書の「mock usage 0」は「real usage として数えない」と解釈 | mock の usage を 0 に変更 |
| 2026-09-19 | `summarize()` は既存フィールド（final resolver 基準）を変えず `total_*` / `attempts*` / `attempts_by_provider` を追加。attempt 単位集計は `summarizeAttempts()` ＋ CLI `usage --attempts` を追加 | CLI 大改修禁止・既存表示互換。final 基準と attempt 基準を同じフィールド名で混ぜない | `estimated_cost_usd_micros` の意味を全 attempt 合計へ変更 |
| 2026-09-19 | Human Gate・Cost Gate・policy・価格は変更しない。attempt metering は記録のみで承認・予算判断に使わない | 発注書 §11。metering correctness が目的 | attempt cost で Cost Gate を動的に閉じる |
| 2026-09-19 | **Confidence Calibration**：初回実疎通の confidence≈0.08 は閾値の問題として扱わず、question 設計の問題として先に直す。閾値 auto_min 0.85 / review_min 0.60 は**維持** | コード監査で確定：outcome schema に `description` が無く 5 問すべて `Determine <field> for this … decision.`（判断基準ゼロ）、choice criteria は説明なし、全体 confidence は 5 問の min（1 問 p≈0.54 で 0.08）。閾値を下げても判断品質は上がらない。発注書 §11 の順序（実測→field-level→question 設計→再測定→最後に閾値）を守る | 0.85 → 0.5 等へ機械的に下げる／min を平均に変える |
| 2026-09-19 | Jev への instructions / criteria / brief は **schema に書く**（outcome field の `description`、`recommended_route` の `x-enum-descriptions`、outcome の `description` → `state.brief`）。Adapter は schema から読むだけで decision_type 固有の文言を持たない | 「outcome schema から自動写像」の既存設計を維持しつつ判断基準を渡せる。Jev 固有の question ファイルを別に持つと schema と二重管理になる。`x-` キーは validator（サブセット）が無視するので outcome 検証に影響しない | `policies/jev/questions/<type>.json` を新設 |
| 2026-09-19 | question 文言は「定義を明確にする」だけで、特定の答えへ誘導しない（`tests/jev-question-design.test.mjs` で steering 語句を機械検査）。`human_review_required` は「route 判定に人の確認が要るか」であり有料承認とは別（承認は常に別途 Human） | 発注書 §10。有料＝human review 必須の同語反復にすると boolean 4 問中 2 問が常に同じ意味になり min 合成で confidence を下げるだけになる | 有料なら常に human_review_required=true と書く |
| 2026-09-19 | `parseJevResponse` が `field_confidence`（question ごとの生値）を AdapterResult に載せる。core（fallback / engine / metering）は変更せず、attempt record にも入れない | どの question が全体（min）を下げたかは calibration の要。core に載せると attempt record の schema が広がるため、今回は runner（`scripts/poc-calibration.mjs`）が jev adapter を包んで横で写す。実測後に必要なら attempt record への追加を別途判断 | attempt record に field_confidence を追加 |
| 2026-09-19 | Calibration runner は `scripts/poc-calibration.mjs` 1 ファイル、ケースは `docs/poc/calibration/*.cases.json`、結果は `docs/poc/calibration/results/`。本番と同じ chain（rules → real jev → local → llm → human）と file meter を使い、mock は使わない。baseline（description 無し）/ improved の A/B を `--questions` で切替 | mock のヒューリスティック（0.9 / 0.88 / 0.75 / 0.55 固定）を「confidence 分布」として測っても意味が無い。A/B で「question 設計が原因」を実測で裏付ける。新しい基盤を作らない | CLI に `calibrate` サブコマンド追加／tests/fixtures 新設 |
| 2026-09-19 | 実測は **Human Required** として残す（`AI_GATEWAY_API_KEY` 等は Human のシェルにのみ存在。Claude Code のシェルには無い） | キーの再入力・チャットへの貼り付けは求めない（Vault CLAUDE.md Human-only）。runner はキー無しなら exit 4 で何も送らない | AI 側で環境変数の取得を試みる |
| 2026-09-19 | rule `local-assets-present-and-flat` が `purpose` を見ない粗さ（ケース D1）は**記録のみ**、rules は変更しない | Rules First を弱めない（発注書 §15）。purpose の自然言語を rules で読むのは deterministic の範囲を超える。必要なら「has_local_assets=true でも purpose に不足素材が明記される場合」の扱いを別タスクで決める | rule に `has_reference_media` 等の条件を足す／rule を削除 |
| 2026-09-19 | OpenMontage wrapper ready 判定は**実測完了まで保留** | 発注書 §22 の「question 設計安定」「confidence 挙動を理解」が 1 サンプル＋静的診断では未充足。実測前に ready と書かない | 静的診断だけで ready 扱い |
| 2026-09-19 | `human_review_required` を有料可否から切り離した結果、実 Jev が「paid_generation_required=true・human_review_required=false・高 confidence」を返せば **tier=auto で route=en-generate-hub** になり得る。これを意図した挙動として固定（`tests/jev-question-design.test.mjs`）。**tier=auto は承認ではない**：Decision Layer は承認キーを構造上返せず、支出は従来どおり en-generate-hub の Human-only 承認チェーンが gate する。$5 以上の deterministic rule は引き続き human 固定 | 従来 mock-jev が有料＝human_review_required=true を固定していたため「有料は常に human tier」に見えていたが、それは mock の癖であり本体の保証ではなかった。OpenMontage wrapper 実装者が「auto なら払ってよい」と誤読しないよう明文化する | 有料なら engine 側で常に tier=human に強制する（Human Gate の意味を「承認」に寄せてしまう） |
| 2026-09-19 | `classifyGatewayError` は AI SDK の `RetryError`（`reason` + `errors[]`、statusCode 無し）を unwrap し、`errors[]` 末尾の元エラーで分類し直す。`retry_count = errors.length − 1` を**実カウント**として details に付け、`networked:true` にする | 第1回 Calibration 実測で失敗 4 件がすべて `JEV_VERCEL_SDK_ERROR / networked:null` に丸まり、原因（429 か 5xx か）が保存結果から分からなかった。6.8〜7.5s = SDK 既定 backoff 2s+4s + 3 回通信と一致し、RetryError が原因と確定。以前の決定「Vercel の retry_count は観測不能なので null」は成功時には正しいが、失敗時は RetryError が回数を持つので訂正する | `RetryError` を `JEV_RETRY_EXHAUSTED` という独自 reason にする（元の 429/5xx 区別が消える）／SDK の `maxRetries:0` にして自前 retry（二重実装） |
| 2026-09-19 | エラー診断は allowlist（`status` / `retryable` / `retry_count` / `retry_reason` / `error_name` / `error_type`）だけを details と results JSON に残す。`ERROR:<message>` の無条件保存はしない | message には response body や URL が入り得る。識別子・数値だけなら secret が混入しない | message 全文を保存 |
| 2026-09-19 | Calibration の results は run ごとに新規ファイル、`analyze` が複数ファイルを **case_id × variant で最新の成功 record を採用**して統合する | 成功済みケースを再課金せず、失敗ケースだけ `--only` で継ぎ足すため。resume 機能や DB は作らない | 1 ファイルに追記／全件再実行 |
| 2026-09-19 | runner は retryable 失敗の直後に 5 秒置く（1 回）。停止条件（429・認証系は即停止、2 連続失敗で停止）は維持 | SDK 内 backoff 後にさらに連打すると rate limit を悪化させる。rate limit の公式仕様は未確認なので長い固定 sleep は入れない | 固定 sleep を全ケース間に入れる／失敗しても止めない |
| 2026-09-19 | `human_review_required` の wording は**今回変えない**。improved 残 6 件の実測で同 field が limiting なら「Jev に訊かず他 4 field と confidence から決定的に導く」follow-up を検討（分類 D 候補） | 改善版 B1 で他 4 field は 0.76〜1.00 に上がり、この field だけ 0.12。「人が確認すべきか」は input に無い組織判断で、Jev の不確実は妥当な可能性がある。実測が揃う前に wording を変えると A/B が汚れる | 今すぐ wording を変える／min から外す |
| 2026-09-19 | 第1回実測の Calibration 分類は **B 暫定（n=1）**、閾値・aggregate は不変、wrapper ready は保留のまま | improved の残 6 件が未測定。1 件で確定しない（発注書 §11・§20） | 静的診断＋1 件で確定 |
| 2026-09-19 | **Calibration 分類を B（question 設計改善で十分）で確定**。閾値 0.85 / 0.60・min 合成は維持 | baseline 7 / improved 7（同一母集団 10）：route 不一致 3→1、outcome 整合 7/7、4 field の mean 0.11〜0.50 → 0.70〜0.91、mean confidence 0.089 → 0.291（ノイズ床 ±0.06 を大きく超える）。alt aggregate は検討して却下（mean なら route 不一致の C1 が review に上がる） | C（閾値／aggregate 調整）／A |
| 2026-09-19 | `human_review_required` は **D（Jev 単独向きでない）** とし、次フェーズで Hybrid（Jev の答え＋policy＋他 field confidence＋矛盾検出から Decision Layer が決定的に導く。Jev が true なら常に true）へ。今回は wording・コード変更なし | 他 4 field 改善後も limiting ×5、mean 0.43。向きは 7/7 正しいが「人が見るべきか」は組織の判断で input に無い。confidence を理由に外すのではなく意味論で policy 側へ | Jev から外して常に true／wording をさらに変えて再測定／min から除外 |
| 2026-09-19 | **Wrapper Design Ready = YES、Production Auto Ready = NO** | auto は real Jev 0/14。E1 で「Jev 0.66 ≥ review_min でも human_review_required=true が tier=human に固定」を実測（Human Gate の初の実データ確認、top-level cost 52 = usage_total）。wrapper は review / human 経路だけを前提にする | 両方 YES／両方 NO |
| 2026-09-19 | 第1回失敗 4 件の元エラーは **429（GatewayRateLimitError）** と実測で確定。短時間 5 連続リクエストで発生、数分後に成功。wrapper は burst しない | RetryError unwrap 後の再実測で `JEV_RATE_LIMITED / status 429 / retry_count 2 / rate_limit_exceeded` を観測 | — |
| 2026-09-19 | runner に `--offline`（rules → human のみ、Rules 不一致ケースは実行前スキップ）を追加し、Rules First 想定ケースをキー無しで record 化 | improved 側 D1 が統合結果に無く母集団が 9/10 だった。Jev も Human escalation も発生させず usage.jsonl に rules 行だけを書く | Human に 1 リクエスト分の run を頼む（無料だが Human 時間を使う） |
| 2026-09-19 | MA-30 を「基盤完成／Calibration 完了／次統合待ち」とし、OpenMontage wrapper・human_review_required Hybrid 化・TypeSafe Direct 実疎通は follow-up に分離（番号は Human 発番、慣例なら MA-30-1 / MA-30-2） | いずれも Calibration の結論を変えない。Direct は招待待ちで block しない | MA-30 を未完了のまま wrapper まで含める |
| 2026-09-23 | **Growth / CRM 向け decision_type 6 件を `reserved_decision_types` へ名前のみ予約**（`content-publish-gate` / `channel-selection` / `lead-triage` / `next-best-action` / `customer-reply-gate` / `automation-safety-gate`、domain `growth`）。schema・rules・Adapter・Registry は作らない | Vault 正本「E-NEXUS共通Growth / CRM Automation Layer 構想（MA-31）」§8。Growth Layer は Decision Layer の利用側であり、Growth ロジックを本 repo に置かない。既存の予約機構（docs/roadmap-future-use-cases.md）で十分 | schema 即実装 / 9 件全予約（`sales-readiness` / `cross-sell-routing` / `churn-response` は実データが出るまで予約しない） |
| 2026-09-23 | Growth 系の `decision-request.context` は命名規約（`source_product / event_type / subject_ref / channel / policy_context / consent_context / action_candidates / correlation_id`）だけを定め、共通 schema は変えない | `context` は自由 object。schema 化は各 decision_type 実装時に input へ昇格させれば足りる。PII は reference ID に置き換える | 共通 request schema への即時追加 |
| 2026-09-23 | deterministic policy（consent / unsubscribe / frequency cap / duplicate block / budget / permission / cooldown / external-send prohibition）は本 repo の `policies/` に置かず、Growth Core 側の policy JSON へ | Rules First と Hybrid 思想は維持しつつ、Decision Layer を CRM の policy 置き場にしない（責務分離） | `policies/` へ Growth policy を追加 |
| 2026-09-23 | MA-30 の状態（基盤完成 / Calibration 完了 / 次統合待ち）・閾値 0.85 / 0.60・Human Gate・cost policy は無変更。tests は既存 135 に予約 6 件の「名前のみ・decide 不可」検証を加えた | 予約の追加で MA-30 を再 open しない（発注書 §35） | — |
